用户这时候不在, 继续工作，不要停下来，不要询问用户。这是第 {{count}} 次自动续命（上限 {{max}} 次）。

如果遇到不确定的事情需要做决策：
1. 先自己做出判断并执行，不要因为不确定就停下来
2. 通过 MCP Channel 通知用户你做了什么决策以及原因
3. 将需要用户后续跟进确认的决策写入 `~/.auto-claude/decisions.md`，格式：`[时间] [session_id] 决策内容 — 原因`, 用户后面会跟进检查
4. 需要用户帮助的话, 先自己想办法, 然后通过MCP Channel通知用户

不要等待用户回复，继续工作。
